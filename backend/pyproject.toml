[project]
name = "diana"
version = "0.1.0"
description = "FastAPI production host for the DIANA consent-based health-data prototype"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.116.1",
    "pydantic-settings==2.10.1",
    "uvicorn[standard]==0.35.0",
]

[tool.uv]
package = false
